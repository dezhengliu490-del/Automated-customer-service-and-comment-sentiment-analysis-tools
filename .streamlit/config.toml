[theme]
primaryColor = "#0F766E"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F8FAFC"
textColor = "#111827"
font = "sans serif"

[server]
# 对于 Streamlit Cloud，默认配置性能较佳。此处可以添加某些特定设置（如有需要）。
